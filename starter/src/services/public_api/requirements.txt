# AWS API Factory - Public API Dependencies
#
# This file contains the Python dependencies for the FastAPI application
# deployed on AWS App Runner.

# Web framework
fastapi>=0.104.0

# ASGI server
uvicorn[standard]>=0.24.0

# Data validation (included with FastAPI, but explicit for clarity)
pydantic>=2.0
